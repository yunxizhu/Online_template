#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const yargs_1 = __importDefault(require("yargs"));
const log_1 = require("./log");
const index_1 = require("./index");
const package_json_1 = require("../package.json");
const build_1 = require("./build");
async function applyPatchesOnVersion(nodeRange, quietExtraction = false) {
    await (0, build_1.prepBuildPath)();
    await (0, build_1.fetchExtractApply)((0, index_1.getNodeVersion)(nodeRange), quietExtraction);
}
async function main() {
    const { argv } = yargs_1.default
        .option('node-range', { alias: 'n', default: 'latest', type: 'string' })
        .option('quiet-extraction', { alias: 'q', type: 'boolean' })
        .version(package_json_1.version)
        .alias('v', 'version')
        .help()
        .alias('h', 'help');
    const { 'node-range': nodeRange, 'quiet-extraction': quietExtraction, } = argv;
    await applyPatchesOnVersion(nodeRange, quietExtraction);
}
main().catch((error) => {
    if (!error.wasReported)
        log_1.log.error(error);
    process.exit(2);
});
//# sourceMappingURL=apply-patches.js.map