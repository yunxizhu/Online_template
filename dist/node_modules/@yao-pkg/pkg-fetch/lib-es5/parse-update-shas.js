"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseAndUpdateShas = void 0;
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const log_1 = require("./log");
function parseAndUpdateShas(inputFile) {
    // Read the input file containing new SHAs
    if (!fs.existsSync(inputFile)) {
        throw (0, log_1.wasReported)(`Input file ${inputFile} does not exist`);
    }
    const input = fs.readFileSync(inputFile, 'utf8');
    const lines = input.split('\n').map(l => l.trim()).filter(Boolean);
    // Read existing shas.txt
    const shasPath = path.join(process.cwd(), 'shas.txt');
    let shasTxt = '';
    try {
        shasTxt = fs.readFileSync(shasPath, 'utf8');
    }
    catch (e) {
        log_1.log.info('shas.txt not found, creating new one');
        shasTxt = '';
    }
    let shasArr = shasTxt.split('\n');
    // Process each line from input
    for (const line of lines) {
        const [sha, patchName] = line.split(/\s+/);
        if (!sha || !patchName) {
            log_1.log.warn(`Skipping invalid line: ${line}`);
            continue;
        }
        // Extract major, os, arch. E.g.: node-v22.20.0-win-x64
        const match = patchName.match(/^node-v(\d+)\.[^ ]*-(.+)-(.+)$/);
        if (!match) {
            log_1.log.warn(`Skipping line with invalid patch name format: ${line}`);
            continue;
        }
        const major = match[1];
        const os = match[2];
        const arch = match[3];
        // Regex for matching line in shas.txt
        const re = new RegExp(`^\\S+\\s+node-v${major}\\.([0-9.]+){2}-${os}-${arch}$`);
        let found = false;
        shasArr = shasArr.map(l => {
            if (re.test(l)) {
                found = true;
                log_1.log.info(`Updating: ${l} -> ${sha}  ${patchName}`);
                return `${sha}  ${patchName}`;
            }
            return l;
        });
        if (!found) {
            log_1.log.info(`Adding new entry: ${sha}  ${patchName}`);
            shasArr.push(`${sha}  ${patchName}`);
        }
    }
    // Write updated shas.txt
    const filteredShas = shasArr.filter(Boolean);
    const updatedContent = filteredShas.length > 0 ? `${filteredShas.join('\n')}\n` : '';
    fs.writeFileSync(shasPath, updatedContent);
    log_1.log.info(`Successfully updated ${shasPath}`);
}
exports.parseAndUpdateShas = parseAndUpdateShas;
// Main execution
if (require.main === module) {
    const args = process.argv.slice(2);
    if (args.length !== 1) {
        log_1.log.error('Usage: parse-update-shas.ts <input-file>');
        log_1.log.error('  input-file: Path to file containing SHA lines in format "<sha> <patch-name>"');
        process.exit(1);
    }
    const inputFile = args[0];
    try {
        parseAndUpdateShas(inputFile);
    }
    catch (error) {
        if (error instanceof Error && !('wasReported' in error)) {
            log_1.log.error(error);
        }
        process.exit(1);
    }
}
//# sourceMappingURL=parse-update-shas.js.map