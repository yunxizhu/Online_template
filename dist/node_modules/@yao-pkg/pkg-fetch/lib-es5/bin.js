#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const yargs_1 = __importDefault(require("yargs"));
const system_1 = require("./system");
const log_1 = require("./log");
const index_1 = require("./index");
const verify_1 = require("./verify");
const package_json_1 = require("../package.json");
async function main() {
    const { argv } = yargs_1.default
        .env('PKG_FETCH_OPTION_')
        .option('node-range', { alias: 'n', default: 'latest', type: 'string' })
        .option('platform', { alias: 'p', default: system_1.hostPlatform, type: 'string' })
        .option('arch', { alias: 'a', default: system_1.hostArch, type: 'string' })
        .option('test', { alias: 't', type: 'boolean' })
        .option('force-fetch', {
        alias: 'f',
        type: 'boolean',
    })
        .option('force-build', {
        alias: 'b',
        type: 'boolean',
    })
        .conflicts('force-fetch', 'force-build')
        .option('output', { alias: 'o', type: 'string' })
        .version(package_json_1.version)
        .alias('v', 'version')
        .help()
        .alias('h', 'help');
    const { 'node-range': nodeRange, platform, arch, test, 'force-fetch': forceFetch, 'force-build': forceBuild, output, } = argv;
    const local = await (0, index_1.need)({
        nodeRange,
        platform,
        arch,
        forceFetch,
        forceBuild,
        output,
    });
    log_1.log.info(local);
    if (test) {
        await (0, verify_1.verify)(local);
    }
}
main().catch((error) => {
    if (!error.wasReported)
        log_1.log.error(error);
    process.exit(2);
});
//# sourceMappingURL=bin.js.map