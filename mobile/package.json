{
  "name": "lianji-android",
  "version": "1.0.0",
  "private": true,
  "description": "联机大厅 Android 客户端（加入端）",
  "scripts": {
    "sync": "npm run sync:js && npx cap sync android",
    "sync:js": "node scripts/sync-shared-js.js",
    "open": "npx cap open android",
    "build:apk": "npm run sync:js && npx cap sync android && cd android && ..\\gradlew.bat assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "mqtt": "^5.10.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
